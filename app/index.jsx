import React from 'react';
import ReactDOM from 'react-dom';
import PasswordValidator from './containers/PasswordValidator';
import './src/styles.scss';

ReactDOM.render(
  <PasswordValidator />,
  document.getElementById('passwordValidatorContainer'),
);
